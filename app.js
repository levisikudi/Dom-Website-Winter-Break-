
// SELECT AND SET THE TITLE
let title = document.querySelector(".title")

// CREATE A changeTitle function that changes the title.textcontent
const changeTitle = (navItem) =>{
   title.textContent = navItem.textContent
}